# 動作範本工作室

Windows 桌面版。雙擊 `FishMarco.exe` 開啟；使用 Windows 內建的 .NET Framework 4.x，不需要 Python 或另外安裝套件。請先儲存並關閉舊版，避免熱鍵互相占用。

## Ctrl+S 儲存與未儲存警示

- 已載入或另存過的動作範本：按 Ctrl+S 直接儲存回目前範本檔案，不再開啟檔案選擇視窗。
- 新範本還沒有檔案路徑：第一次按 Ctrl+S 開啟另存視窗。選定位置並成功儲存後，之後 Ctrl+S 會直接更新該檔案。
- 上方「另存範本」仍可選擇其他名稱或位置；另存成功後，新檔案成為之後 Ctrl+S 的儲存目標。
- 關閉程式、建立新範本或載入另一個範本時，如果目前動作範本有未儲存變更，會提示「是否確定離開範本？未儲存的變更將不會儲存」。預設為「否」；選否返回編輯，選是放棄未儲存變更後離開。
- 已儲存且沒有變更時不會跳警示。新增、刪除（包含刪除全部）、修改、備註、排序與循環設定都會納入檢查。儲存失敗不會標記為已儲存。
- 上方新增／修改欄位仍需按「加入動作」或「套用至所選動作」才會成為範本內容；完整修改視窗先按「儲存修改」。Ctrl+S 儲存的是動作清單及循環設定。
- 此快捷鍵與離開警示適用於主視窗的動作範本。範本組合仍使用其視窗的「儲存組合」。按鍵捕捉期間 Ctrl+S 會被當成要捕捉的組合鍵，不會觸發檔案儲存。

## 編排與執行

1. 選「滑鼠點擊」、「鍵盤按壓」或「等待」。
2. 新增滑鼠動作：選擇「按鈕」（左鍵／右鍵／中鍵），按住十字並拖到目標位置放開，再按「加入動作」。新增區域不再顯示 X／Y 輸入或 3 秒取位。已新增動作的清單及修改視窗仍保留 X／Y，可直接修改數值。支援負座標的多螢幕位置；橘色十字交點為目標座標，Esc 取消拖曳。
3. 鍵盤動作：預設為單鍵 `Space`。可直接點 `Space`、`Enter`、`Delete` 等常用按鍵按鈕，或按「捕捉按鍵」後在鍵盤按一下所需按鍵。組合鍵則一起按，例如 Ctrl+S；也可直接在欄位輸入 `A`、`Enter`、`Ctrl+Shift+S`。常用按鈕會直接替換為單鍵，不會保留先前的 Ctrl 等修飾鍵。捕捉時 Esc 取消；若要設定 Escape，使用常用按鈕。F9、F10 為程式保留熱鍵。捕捉只在輸入欄位取得焦點時進行；Windows 攔截的系統快捷鍵可改用文字輸入。
4. 設定「按住時間」與「動作後等待」，按「加入動作」。**動作後等待使用秒（sec），預設 1 sec**，可輸入 0.5、1.25 等小數，範圍為 0～86400 sec，最多三位小數。載入舊範本或選取既有動作時保留實際時間，例如 500 ms 顯示為 0.5 sec。按住時間、循環間隔及範本切換等待仍使用毫秒（ms）。「等待」動作只使用「動作後等待」。
5. 選取清單中的動作可修改，再按「套用至所選動作」；可上移、下移或刪除。橘色預覽不會自動修改已加入的動作，須按套用或加入。
6. 設定循環次數（0 代表持續到手動停止）與循環間隔。每一輪都依序執行所有動作；每個動作完成後等待指定時間，兩輪之間額外等待循環間隔。最後一輪完成後不再等待循環間隔。
7. 按「另存範本」，每組流程存成獨立 JSON 檔；日後以「載入範本」開啟。範本會保存動作順序、座標、按鍵、按住時間、各動作等待、循環次數與循環間隔。
8. 按 **F9** 或開始按鈕，主螢幕正中央會依序顯示 **3 → 2 → 1**，每個數字顯示 1 秒。在倒數期間切換到要操作的視窗；倒數視窗不取得焦點。按 **F10** 可在倒數或執行期間停止，也可以使用程式內的停止按鈕。

## 拖曳排序與 Shift 多選

- 單筆：按住清單中的動作列，上下拖曳到藍色插入線的位置後放開。
- 多筆：先點第一筆，再按住 Shift 點最後一筆，選取整個範圍。從任一反白列開始拖曳，即可整批移動；拖曳時可放開 Shift 或繼續按住。
- 整批移動會保留所選動作彼此的原始順序，移動後仍維持多選。序號與螢幕十字編號會一起更新。
- 原有「上移／下移」按鈕也支援多選移動；「刪除所選」會刪除所有選取列。同類型多選後可使用「批次修改」。
- 執行期間無法改動排序；排序後記得另存範本。

## 備註（選填）

每種動作都有「備註」欄位，可填文字、數字、符號或多行內容，也可留空。新增時在上方輸入；既有動作可右鍵「修改動作」編輯備註，再按「儲存修改」。備註只供閱讀，不會當成鍵盤輸入或影響動作執行。

備註會隨動作複製、移動及範本／範本組合保存。舊範本沒有備註時會顯示空白，仍可正常載入。

範本不會自行啟動。執行中會鎖定編輯，停止時會嘗試釋放程式按住的滑鼠按鈕／鍵盤按鍵。若 F10 被其他程式占用，程式會禁止開始，需關閉占用熱鍵的程式後重新開啟。

下方開始／停止按鈕保留原有文字與排列，字體放大；開始按鈕以淺綠色顯示。單一範本與範本組合皆適用。

## 右下角執行提示

倒數結束後，主螢幕右下角顯示「按鍵精靈執行中」，下方顯示目前範本名稱與剩餘循環次數。剩餘次數包含正在執行、尚未完成的這一輪，完成一整輪後減一。組合切換到下一個範本時會更新名称與次數；無限循環顯示「持續循環」。新範本尚未儲存時顯示「未命名範本」。

提示不取得焦點，滑鼠可穿透；停止、完成或執行錯誤時自動消失。

## 雙擊儲存格直接編輯

在清單中用滑鼠左鍵雙擊要修改的格子，例如「動作後等待（sec）」，直接輸入秒數，按 Enter 或點其他格子完成；Esc 取消。可編輯動作類型、適用的 X／Y、按鍵／滑鼠按鈕、按住時間、動作後等待及備註。序號自動產生，不可手動改；鍵盤動作的 X／Y 等不適用欄位也無法編輯。

座標及按住時間須為允許範圍內的整數；動作後等待可輸入最多三位小數的秒數。錯誤輸入不會覆寫原設定，下方會提示原因。按鍵可輸入 `Space`、`Enter` 或 `Ctrl+S` 等，滑鼠按鈕輸入「左鍵／右鍵／中鍵」。若要使用拖曳十字、按鍵捕捉或編輯多行備註，仍可右鍵「修改動作」開啟完整視窗。修改後記得另存範本。

## 同類型動作批次修改

1. 用 Shift 選取至少兩個已新增的動作。
2. 按「批次修改」，或在反白範圍內按右鍵選「批次修改同類型動作」。
3. 只有全部都是滑鼠、全部都是鍵盤、或全部都是等待動作時才能批次修改；混合類型會被阻止。
4. 勾選要修改的欄位，再填入共用值。未勾選的欄位各自保留原內容，避免不小心把不同座標或備註變成相同。
5. 按「套用至 N 個動作」一次完成；取消則不修改。勾選備註後留空，可清空所有所選動作的備註。

批次修改包含該類型適用的座標、按鍵／滑鼠按鈕、按住時間、動作後等待及備註，不會改變動作類型或順序。修改後請另存範本。

## 動作類型欄底色

- 滑鼠點擊：淺藍色。
- 鍵盤按壓：淺綠色。
- 等待：淺黃色。

配色只套用於「動作」類型欄，其餘欄位保留原色。反白選取時使用系統選取色，以便識別多選範圍；取消選取後恢復類型欄底色。

## 範本組合：A → B → C

1. 先把每個動作範本各自存成 JSON，例如 A.json、B.json、C.json。
2. 在主視窗按「範本組合…」，再按「加入範本…」選取檔案，可一次加入多個。
3. 選取 A，將「所選範本次數」設為 1000 並按「套用次數」。B 設為 1000、C 設為 500。
4. 用「上移／下移」排列 A → B → C；依需要設定「範本切換等待」，預設 1000 ms。
5. 按「儲存組合…」存成 `.sequence.json`。未來以「載入組合…」開啟；同一次開啟程式期間，關閉再開組合視窗也會保留清單。
6. 按「執行組合」，主螢幕中央顯示 3 → 2 → 1 後依序執行。整個組合只在開始時倒數一次，切換範本不會重複倒數。**F10 停止整個組合**；停止或遇到錯誤後不會繼續下一個範本。

組合中的次數直接取代原範本的循環次數，不會相乘。例如 A 原本設定 10 次，組合設定 1000 次，實際執行 A 的動作清單 1000 輪。每輪保留該範本的動作後等待與循環間隔；換下一個範本時另加「範本切換等待」，最後一個範本結束後不加此等待。組合中每個範本次數須為 1～1000000，不提供無限循環，以確保後續範本可執行。

組合檔包含加入當時各範本的完整內容，原始 JSON 移動後仍可執行。之後若修改了原始範本，需在組合中刪除該列並重新加入，才會使用新版內容。組合檔與單一動作範本使用各自的載入按鈕。

## 螢幕點位與動作序號

- 清單第一欄為「序號」，所有動作依執行順序編號；加入、移動、刪除或載入後會自動重新編號。
- 已加入的滑鼠動作會在螢幕上顯示藍色十字與對應序號。例如第 1、3、5 個動作是滑鼠點擊，點位會標示 1、3、5。
- 多個動作使用完全相同的座標時，十字旁合併顯示，例如「1, 3」。橘色表示正在編輯的座標預覽。
- 十字的交點就是實際點擊座標。標記視窗設為不取得焦點、滑鼠可穿透，方便繼續操作桌面。
- 可取消上方「顯示螢幕點位」來隱藏全部標記。執行及擷取倒數時暫時隱藏，結束後依勾選狀態恢復；關閉程式會移除標記。
- 舊版 JSON 範本可直接載入，會依原有清單順序補上序號，重新儲存後一併保存。

## 右鍵複製與修改

在清單中對要操作的動作按右鍵，即可選擇：

- **複製動作**：在該動作下一筆插入副本，保留動作內容與時間，序號自動更新。修改副本不會影響原動作。
- **修改動作**：開啟專用修改視窗。只有按「儲存修改」才會套用，按「取消」保留原內容。左鍵雙擊則直接編輯該格內容。修改後仍須另存範本才能保存到 JSON 檔案。

滑鼠動作可直接輸入 X、Y 數字，或按住修改視窗中的藍色十字，拖到螢幕目標位置後放開。拖曳期間程式視窗會暫時淡化，橘色十字跟隨滑鼠，放開後把位置填入 X、Y。交點即為目標座標。Esc 可取消此次拖曳並還原拖曳前的座標。

滑鼠按鈕使用下拉選單；鍵盤使用按鍵捕捉、常用按鈕或文字輸入。按住時間使用 ms，動作後等待使用 sec，皆可直接输入數字。修改視窗開啟期間不會透過 F9 啟動動作循環。

## 依動作類型切換欄位

- **滑鼠點擊**：新增時顯示按鈕選擇與拖曳十字，另有共用的按住時間、動作後等待與備註；已新增動作仍保留 X／Y。
- **鍵盤按壓**：只顯示按鍵／組合鍵、按住時間及動作後等待。
- **等待**：只顯示動作後等待時間。

清單中不適用的資料以「—」呈現，避免把鍵盤動作誤認為有滑鼠座標。

## 顏色掃描小工具

按上方工具列的「顏色掃描小工具」開啟。它是獨立視窗，可以和動作編輯同時開著；再按一次只會把既有視窗帶回前景。

它的用途是**盯住畫面上某個顏色**：把感知上足夠接近的像素畫成高亮色塊，讓你一眼看到那個顏色出現在哪裡。

### 填色碼

按「＋ 新增色碼組」，填入色碼（例如 `#2E3045`）和誤差，最多 20 組。每一組有自己的誤差和自己的高亮色。

**染色色碼可以直接填進去，不必先吸色。** 實測染色色碼和它在畫面上的顏色最多只差 5.6，在預設誤差內。

### 誤差要設多少

誤差就是「多接近才算同一個顏色」。

| 你的顏色從哪來 | 誤差 |
| --- | --- |
| **填色碼**（主要用法） | **6**（預設，不用動） |
| **吸色取的** | **3** |
| 上限 | **15** |

**掃不到就往上加一點，看到雜點就往下調。** 不用去算。

為什麼兩種來源不一樣：吸色吸到的就是畫面上那個顏色，只要蓋住同一件東西的深淺就夠，實測三個材質都只要 1.5 左右，3 綽綽有餘。但色碼和它在畫面上的顏色本來就有距離（布 4.7、皮革 5.6、金屬 2.1），所以填色碼要 6 —— 設 5 會漏掉皮革自己那件東西。

**皮革和布分不開，除非你吸色。** 皮革色碼離自己的畫面顏色 5.57，離布的畫面顏色卻只有 5.55 —— 布比它自己還近，所以填皮革色碼時沒有任何誤差值能只留皮革。要分開就吸色（3 就夠），或把掃描方塊縮小到只框住你要的那一件。

### 誤差過大會破圖

徵兆有兩階段：

1. 原本一整塊的色塊**碎成散落各處的雜點** —— 這是最早出現的警訊
2. 再大下去整個範圍糊成一片，高亮蓋滿畫面，等於什麼都沒找到

看到雜點就調小。反過來，目標明明在畫面上卻只有零星幾格命中，那是誤差太小。

**上限 15 是量出來的，不是感覺。** 以布為中心，一個跟它毫無關係的中性深灰 `#474747` 只有 15.7 —— 那種灰在介面、陰影、地面到處都是，所以誤差 16 就開始撈到無關的東西。

**誤差不是越大越安全。** 如果你想「開大一點應該就能找到顏色相近的其他裝備」—— 那個做不到：不同材質的色碼差 10 到 43，而 15 就破圖了。跨材質配色要靠比對色盤，不是把這個數字往上開。

### 順序有意義

比對**由上往下**，每個像素只認第一個命中的色碼組。上方那組的誤差若已涵蓋下方那組的色碼，下方就完全不會有命中。兩組顏色相近時，把誤差調小，或把想優先看到的放上面。

### 誤差預覽

按「誤差預覽」會開一個視窗，把誤差邊界上最極端的顏色畫出來 —— 畫出來的每個顏色都會被判定為命中，所以色偏有多嚴重一眼就看得到。它同時會算出哪幾組規則互相吞掉。視窗可以一邊看一邊調誤差，改完再按一次。

有些色票是虛線框，代表它其實在誤差外（某些方向推出去就超出螢幕能顯示的顏色範圍了）。預覽照實標示，不會假裝它命中。

### 吸色

按色碼組那一列的「吸色」之後：

1. 掃描方塊收起，工具視窗與主視窗最小化讓開
2. 游標旁跟著一個小色票，即時顯示游標下的顏色與色碼
3. 按左鍵取色，Esc 或右鍵取消，之後視窗還原

畫面不會被凍結或蓋住，顏色是在你按下左鍵的那一刻才讀。吸色期間所有滑鼠按鍵都會被吃掉，不會順手在目標程式上點到東西。逾時沒取色會自動取消並還原視窗。

### 掃描參數

| 參數 | 預設 | 說明 |
| --- | --- | --- |
| 掃描間隔 | 500 ms | 也是下限。再快閃爍會刺眼，掃描也可能讀到自己的殘影 |
| 取樣間隔 | 1 px | 每個像素都看，不漏細小色塊。調大會漏 |
| 色塊大小 | 3 px | 高亮方塊的邊長。看得到又畫得準的折衷 |

**高亮閃爍**這個選項是在兩件事之間選：

- **勾選（預設）**：高亮亮暗交替（1 秒一次）。好處是**你自己截圖拍得到高亮**。
- **取消**：請系統把高亮視窗排除在螢幕擷取之外，所以**完全不閃**，掃描頻率也變成兩倍。代價是你自己截圖也拍不到高亮。需要 Windows 10 2004 以後的版本；系統不支援時會自動退回閃爍，並在狀態列說明。

按 **F7** 開始掃描、**F8** 結束，也可以用按鈕。「鎖定掃描範圍」之後方塊不能再被拖動或縮放，避免誤觸。「置中於主螢幕」把方塊放回畫面中央。色碼組與掃描參數會自動保存，下次開啟沿用。

## 使用範圍

- 使用絕對螢幕座標定位，不會辨認圖片或追蹤視窗；視窗位置、解析度、縮放或螢幕配置改變後應重新定位。
- 鍵盤按鍵送到當下取得焦點的視窗。先用單次循環與簡單動作確認位置和焦點。
- 一般權限的程式可能無法操作以系統管理員權限執行的視窗；Windows 安全桌面及部分禁止模擬輸入的程式不支援。
- 十字拖曳用於設定點擊位置；目前不包含自動拖曳物件、整段操作錄製、圖像辨識或條件分支。
- 顏色掃描只讀螢幕上實際顯示的像素，不會去讀取遊戲或其他程式的內部資料；畫面被其他視窗遮住的部分掃不到。掃描結果只是高亮顯示，不會自動點擊。

## 原始碼與驗證

附上原始碼與 `build.cmd`，可自行重新編譯。已通過原有功能，以及秒數精度與範圍、舊範本等待時間還原、只套用勾選欄位、批次清空備註、混合類型阻擋、無效批次不修改原資料及不同底色測試，並檢查離屏介面。範本中的 Delay 仍以毫秒儲存，以維持舊檔相容性；介面統一轉換為秒。執行測試使用模擬輸入，沒有向桌面送出實際鍵鼠動作。

已驗證 Ctrl+S 原路徑覆寫、載入後儲存、變更與刪除偵測、還原後無變更、儲存失敗保留未儲存狀態及原檔案路徑。

顏色掃描的部分，上面那些誤差建議不是估的，是拿實際從遊戲畫面吸到的三組材質資料（布、皮革、金屬）量出來的，並且直接釘成測試 —— 數字若因為改程式而跑掉，自我測試會失敗。但只有三個染色、每個材質一個，所以那些建議是**起點而不是定論**：掃不全就往上加，出現雜點就往下調。

介面排版已分成「範本管理」「新增動作」「動作排序與修改」「循環與執行」四個有標題與框線的區塊。欄位標籤與輸入框會成組排列，各列依內容自動增加高度；上方內容不足時可捲動，下方排序及開始／停止區保持可見。
